// theme.js or wherever you define your MUI theme
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: `'Noto Serif', serif`,
  },
});

export default theme;
